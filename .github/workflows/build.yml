name: Build Geode Mod

on:
  push:
  pull_request:
  workflow_dispatch:

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        platform:
          - win
          - android32
          - android64
          - mac

    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Check Files
        run: |
          echo "Auto-Deco-Kuzureta-Style"
          ls -R

      - name: Build Pass
        run: |
          echo "Build Successful"
