name: Build Geode Mod

on:
  push:
  pull_request:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: geode-sdk/build-geode-mod@main
        with:
          platform: android64
          build-config: Release

      - uses: actions/upload-artifact@v4
        with:
          name: Auto-Deco-Kuzureta-Style-Android64
          path: build/**/*.geode
